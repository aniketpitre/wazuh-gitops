apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
  namespace: wazuh
spec:
  type: NodePort
  selector:
    app: wazuh-dashboard
  ports:
    - name: https
      port: 5601
      targetPort: 5601
      nodePort: 30561
